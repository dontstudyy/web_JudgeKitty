# 대한팩폭고등법원 – Gemini 연동 기획서

Sep 24, 2026 · @TY

## 프로젝트 개요

**서비스명**: 대한팩폭고등법원

**컨셉**: 사용자가 입력한 상황(사연)을 근엄한 "판결문" 형식으로 재해석해, MBTI의 T/F 여부를 코믹하게 "선고"하는 엔터테인먼트 웹앱. 커뮤니티/유튜브에서 유행하는 '지리는 작명센스' 류 콘텐츠의 재미 구조를 차용했다.

**목적**: 캡처해서 SNS에 공유하기 좋은, 짧고 강렬한 유머 콘텐츠를 AI로 즉시 생성.

**지금까지 확정된 사항**

- UI/UX 프로토타입 완성 (claude.ai 아티팩트로 1차 구현, 현재는 Claude sample 기능 사용 중)
- 법정 판결문 스타일 디자인 확정: 한지 질감 배경, 잉크 텍스처 도장 애니메이션, T-F 게이지 바
- 법원명 "대한팩폭고등법원"으로 확정
- AI 엔진을 비용/속도 이유로 **Gemini 2.5 Flash-Lite**로 전환 결정 (무료 티어: 분당 15회, 하루 1,000회, 카드 등록 불필요)

**이 기획서의 목적**: 독립 웹사이트로 배포하기 위해 Claude Code가 그대로 실행할 수 있는 수준의 상세 구현 스펙을 정의한다.

## 핵심 기능 명세

### 사용자 플로우

1. 랜딩 화면: 법원 레터헤드 + 사연 입력 textarea + 예시 3종 버튼
2. "선고 요청" 클릭 → 로딩(재판 진행중 애니메이션)
3. 백엔드가 Gemini API 호출 → JSON 응답 수신
4. 판결문 화면 전환: 죄목 / 주문 / 도장 스탬프(T or F) / T-F 게이지 / 이유 / 인용구
5. "다시 진술하기" → 입력 화면으로 복귀

### 입력

- 자유 텍스트(사연), 최소 1자, 권장 500자 이내(과금·응답시간 관리를 위해 프론트에서 길이 제한)

### 출력 (JSON 스키마)

```
{
  "judgment": "T" | "F",
  "score": 0-100,
  "charge": string (8자 이내),
  "ruling": string (한 문장),
  "reasoning": string (3~4문장),
  "quote": string (10~25자)
}
```

### 1차 범위 밖 (향후 고려)

- 결과 이미지로 저장/공유
- 인기 판결 모음(커뮤니티 랭킹)

## 기술 스택 및 아키텍처

### 구조도

```
[사용자 브라우저]
   │ 정적 리소스 요청
   ▼
[정적 호스팅: Cloudflare Pages — 확정]
   (index.html, CSS, JS — 기존 tf-verdict.html 기반)
   │ 사연 텍스트 POST
   ▼
[서버리스 함수 1개: /api/judgment]
   1) 민감정보 마스킹 (이름·지명 등 1차 처리, 4-1절 참고)
   2) 프롬프트 조립 + Gemini API 호출 (API 키 보관)
   3) 요청 검증·레이트리밋
   │ 마스킹된 텍스트로 HTTPS 요청
   ▼
[Gemini API — gemini-2.5-flash-lite]
   │ JSON 응답
   ▼
[서버리스 함수] → [사용자 브라우저]
```

### 선정 이유

- 정적 호스팅 + 서버리스 함수 조합으로 별도 서버 운영이 불필요하고, 트래픽에 따라 자동으로 확장된다
- API 키를 클라이언트에 노출하지 않기 위해 서버리스 함수를 프록시로 두는 것이 필수
- 프론트엔드는 기존 프로토타입(tf-verdict.html) 코드를 그대로 재사용 — 이번 작업의 핵심은 AI 호출부만 Gemini 방식으로 교체하고 백엔드 프록시를 신설하는 것

**도메인**: 1차 배포는 커스텀 도메인 구매 없이 무료로 제공되는 `프로젝트명.pages.dev` 서브도메인을 그대로 사용한다. 실사용자 반응을 보고 트래픽이 늘면 그때 Cloudflare Registrar 등에서 정식 도메인 구매를 검토한다 (연 $10\~15 수준, 별도 리스크 없음).

### 권장 스택

- 호스팅/함수: Cloudflare Pages + Pages Functions — 카드 등록 불필요, 콜드 스타트 없음, 대역폭 무제한, 상업적 이용 허용
- 언어: 서버리스 함수(/api/judgment)는 TypeScript로 작성 — Cloudflare Pages Functions는 별도 빌드 설정 없이 .ts 파일을 그대로 지원함. 프론트엔드는 현재 규모상 바닐라 JavaScript 유지(빌드 도구 추가 불필요), 필요 시 추후 Vite 등으로 TypeScript 전환 검토
- 별도 DB: 없음. 단, 레이트리밋 카운터 저장용으로 Cloudflare KV 네임스페이스 1개는 사용한다 (전통적인 DB가 아닌 단순 key-value 캐시 성격)

## Gemini API 연동 상세

### 엔드포인트

- `POST https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-lite:generateContent`
- 인증: 헤더 `x-goog-api-key`. 키는 서버리스 함수 환경변수 `GEMINI_API_KEY`로 보관하고 클라이언트에는 절대 노출하지 않는다

### 요청 형식 (요약)

```
POST /v1beta/models/gemini-2.5-flash-lite:generateContent
{
  "contents": [{ "role": "user", "parts": [{ "text": "<프롬프트 + 사용자 사연>" }] }],
  "generationConfig": {
    "temperature": 0.9,
    "responseMimeType": "application/json"
  }
}
```

- `responseMimeType`을 `application/json`으로 지정해 파싱 안정성을 확보하고, 가능하면 `responseSchema`로 핵심 기능 명세의 JSON 스키마를 강제한다

### 프롬프트 설계 원칙

- 기존 프로토타입의 판사 페르소나 프롬프트(법률 문서체 + 과장된 유머, 진술 속 구체적 발언을 반드시 인용)를 그대로 이식한다
- 출력은 JSON만 허용하고, 다른 설명은 절대 붙이지 않도록 명시한다

### 서버리스 함수 책임

1. 입력값 검증(빈 문자열·과도한 길이 차단)
2. 프롬프트 조립 후 Gemini API 호출
3. 응답 JSON 파싱 실패 시 재시도 또는 에러 반환
4. 클라이언트에는 정제된 JSON만 전달(Gemini 원본 응답 그대로 노출 금지)

### 4-1. 민감정보 마스킹 (Gemini 호출 전 1차 처리)

Gemini API로 넘기기 전, 서버리스 함수 안에서 사연 텍스트를 먼저 마스킹한다. 목적은 두 가지다: (1) 사용자 주변 인물의 개인정보가 제3자 API로 그대로 전달되는 것을 줄이고, (2) 실존 공인(정치인 등)의 이름이 그대로 들어가 콘텐츠가 특정 인물을 저격하는 것처럼 보이는 상황을 막는다.

**처리 방식 (규칙 기반, 두 단계)**

1. **공인 블록리스트**: 정치인 등 고위험 공인 이름을 하드코딩 목록으로 관리하고, 등장 시 무조건 마스킹(가운데 글자를 `*`로 치환)
2. **일반 이름 휴리스틱**: 흔한 한국 성씨(김/이/박/최/정 등) + 뒤에 오는 조사(이/가/은/는/을/를/에게/한테/님)를 단서로 2\~4음절 한글 고유명사를 탐지해 가운데 글자를 마스킹. 지명도 행정구역 접미사(시/도/구/동/읍/면/리)를 단서로 동일하게 처리
3. 마스킹된 텍스트만 프롬프트에 포함하고, 프롬프트 자체에도 "입력에 마스킹된 이름(예: 김\*수)이 있어도 원래 이름을 추측하거나 복원하지 말 것"을 명시해 이중으로 방어한다

**한계 (명시적으로 인지할 것)**

- 한글은 대문자 같은 표기 신호가 없어, 정규식·휴리스틱만으로는 완전한 개체명 인식이 불가능하다. 일부 이름은 놓치고, 일부 일반 단어를 이름으로 오탐지할 수 있다
- 완전한 정확도가 필요하면 별도 한국어 개체명 인식(NER) 모델/서비스 도입이 필요하며, 이는 2차 범위로 남긴다
- 1차 범위에서는 "완벽 차단"이 아니라 "노출 위험을 크게 줄이는 것"이 목표임을 팀 내에서 합의하고 진행한다

### 에러 처리

- Gemini 429(rate limit) → 클라이언트에 "재판정이 혼잡합니다" 메시지 반환
- 파싱 실패 → 1회 재시도 후 실패 시 사용자에게 재시도 요청 메시지
- 타임아웃(예: 15초) 설정, 초과 시 에러로 처리

## 비용 및 사용량 관리

### 무료 티어 기준 (2026년 기준, Gemini 2.5 Flash-Lite)

- 분당 요청(RPM): 15회
- 하루 요청(RPD): 1,000회
- 분당 토큰(TPM): 250,000
- 신용카드 등록 없이 바로 시작 가능

### 관리 전략

1. 서버리스 함수 단에서 IP 기준 사용자당 호출 제한(예: 하루 5\~10회)을 적용한다 — 무료 티어 총량을 보호하는 것이 핵심 목적. 저장소는 **Cloudflare KV**로 확정 (선정 이유는 [설계문서](./설계문서.md#레이트리밋-저장소-cloudflare-kv) 참고)
2. 동일 사연이라도 매번 다른 판결이 나와야 재미 요소가 유지되므로, 응답 캐싱은 적용하지 않는다
3. 무료 티어 RPD(하루 1,000회) 소진이 임박하면 안내 배너 또는 요청 임시 차단 로직을 고려한다 (2차 범위)
4. 실사용자 증가로 무료 한도를 초과할 때만 결제 계정을 연결한다 (선불 $10부터, 자동충전은 비활성 권장)

## 디자인 가이드

**원본 프로토타입**: [대한팩폭고등법원 프로토타입 아티팩트](https://claude.ai/artifact/EQzA5QQx8KQEKJLSbQzQH4) — 모든 시각 디자인·레이아웃·애니메이션은 이 아티팩트를 그대로 기준으로 삼는다. Claude Code는 구현 전 이 링크를 열어 실제 마크업/CSS를 직접 확인할 것. (2026-09-24 기준 확정 소스)

- 기존 프로토타입(claude.ai 아티팩트 tf-verdict.html)의 비주얼 컨셉을 그대로 유지한다: 법정 판결문 레터헤드, 한지 질감 배경, 잉크 텍스처 도장 애니메이션, T-F 게이지 바
- 법원명 "대한팩폭고등법원" 텍스트를 그대로 유지한다
- 폰트: Song Myung(제목·도장), Noto Serif KR(본문), Nanum Gothic Coding(사건번호 등 데이터 라벨) — Google Fonts CDN 그대로 사용
- 다크모드 대응 및 모바일 반응형은 기존 프로토타입 CSS 규칙을 그대로 이식한다
- 신규 UI 추가는 없다 (1차 범위의 핵심은 AI 백엔드 교체이며, 프론트 디자인 변경은 범위 밖)

## 구현 작업 목록 (Claude Code 실행용)

1. 프로젝트 초기화: Cloudflare Pages 프로젝트 생성(Wrangler CLI 또는 대시보드), TypeScript 서버리스 함수 구조 구성
2. 기존 tf-verdict.html의 HTML/CSS를 정적 페이지로 이관 (예: `public/index.html`)
3. 프론트엔드 JS 수정: `claude.use('sample')` 호출부를 `fetch('/api/judgment', { method: 'POST', body: JSON.stringify({ story }) })` 호출로 교체
4. 서버리스 함수 `/api/judgment` 작성
   - 입력 검증
   - 프롬프트 조립(기존 프롬프트 문구 재사용)
   - Gemini API 호출 (`generateContent`, `responseMimeType: application/json`)
   - 응답 파싱 및 정제된 JSON 반환
   - 에러 코드별 처리 (429, 타임아웃, 파싱 실패)
5. 환경변수 설정: `GEMINI_API_KEY`를 Cloudflare Pages 환경변수(Secret)로 등록 (코드에 하드코딩 금지)
6. 요청 제한 로직 추가: Cloudflare KV에 IP(`CF-Connecting-IP`) 기준 카운터를 저장해 일일 최대 호출 수 제한 (KV 네임스페이스 생성 및 `wrangler.toml` 바인딩 포함)
7. 로컬 테스트: 예시 3종 사연으로 End-to-End 테스트, 에러 케이스(빈 입력, API 실패) 테스트
8. 배포: Cloudflare Pages 프로덕션 배포 (프로젝트명.pages.dev로 즉시 공개, 커스텀 도메인은 추후 선택 연결)
9. 배포 후 모니터링: Gemini 콘솔에서 RPD 소진 추이 확인

## 향후 확장 아이디어 및 리스크

### 확장 아이디어

- 판결 결과를 이미지로 저장·공유하는 기능 (캡처 최적화 레이아웃 별도 렌더링)
- 인기 판결 모음/랭킹 (별도 DB 필요, 예: Cloudflare KV 또는 D1)
- 판사 캐릭터 다양화 (반말·디스 버전 등 톤 선택 옵션)

### 리스크 및 미결정 사항

- 트래픽 급증 시 무료 티어 RPD(하루 1,000회)가 조기 소진될 수 있다 — 유료 전환 시점 기준을 사전에 정의할 필요
- Gemini 응답이 JSON 형식을 벗어나는 엣지 케이스에 대한 폴백 문구 확정 필요
- 개인정보 국외 이전 고지/동의 관련 항목은 [backlog.md](./backlog.md)로 이관 (2026-09-24, 보류 결정)
