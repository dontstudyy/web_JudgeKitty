# 대한팩폭고등법원 – 설계 결정 기록 (ADR)

기획서([`(기획서) 대한팩폭고등법원.md`](<./(기획서) 대한팩폭고등법원.md>))에서 갈릴 수 있는 선택지가 있었던 지점들에 대해, 무엇을 왜 선택했는지 기록한다. 기획서는 "무엇을 만들지", 이 문서는 "왜 이렇게 만들지"를 담당한다.

---

## 호스팅/함수: Cloudflare Pages

**결정**: Cloudflare Pages + Pages Functions

**이유**:
- 카드 등록 없이 바로 시작 가능, 콜드 스타트 없음, 대역폭 무제한, 상업적 이용 허용
- 정적 호스팅과 서버리스 함수(`/api/judgment`)가 하나의 배포 단위로 묶여 별도 인프라 구성이 불필요
- `.ts` 파일을 별도 빌드 설정 없이 Pages Functions가 그대로 지원

**비교 근거 (검토 당시 조사한 데이터)**:

| 후보 | 발목 잡는 지점 |
|---|---|
| **Vercel (Hobby, 무료)** | 무료 플랜은 약관상 **비상업적 용도 전용**. 광고·후원 결제 등으로 수익화하는 순간 유료 플랜(Pro, $20/월)으로 전환해야 함 |
| **Netlify (Free)** | 크레딧제 과금 — 월 300크레딧이 대역폭 약 30GB 상당으로 환산됨. 함수 호출도 같은 크레딧 풀을 공유해 예측이 어려움 |
| **Firebase (Spark)** | **치명적 결함**: Cloud Functions는 무료(Spark) 플랜에서 아예 배포가 안 됨. 사용량이 0이어도 카드를 등록하고 Blaze(종량제) 플랜으로 전환해야만 함수를 올릴 수 있음 — "카드 등록 없이 시작"이라는 전제와 정면으로 배치되어 1순위에서 제외 |
| **Render (Free)** | 무료 웹서비스가 **15분 미사용 시 슬립**되고, 재기동에 30~60초 콜드 스타트 발생. 첫 방문자가 "고장났나?" 싶을 정도로 대기해야 함 |
| **Cloudflare Pages** ← 채택 | 위 네 가지 문제(카드 필요, 크레딧 예측불가, 함수 배포 불가, 콜드 스타트) 중 어느 것도 해당 없음. 다만 빌드 500회/월·Workers 요청 10만/일 한도가 **계정 전체로 공유**된다는 제약은 있어, 유사 사이트를 여러 개 운영할 경우 감안해야 함 |

**대안이었던 것**: Vercel — 기능적으로는 대동소이하나, 위 표의 비상업적 이용 제한과, Cloudflare 쪽이 레이트리밋용 KV를 같은 생태계 안에서 바로 붙일 수 있다는 점에서 Cloudflare Pages를 최종 선택.

---

## AI 모델: Gemini 2.5 Flash-Lite

**결정**: 판결문(주문/이유/죄목/인용구) 생성에 Gemini 2.5 Flash-Lite 사용

**이유**:
- 무료 티어: 분당 15회, 하루 1,000회, 카드 등록 없이 바로 시작 가능 — "비용 폭탄 걱정 없이 시작"이라는 요구사항에 부합
- 유료 전환 시에도 $0.10/$0.40 (100만 토큰당) 수준으로 저렴. 판결문 1건당 입력+출력 합쳐 500~1,000 토큰 정도라 실사용 비용은 사실상 미미
- 이 서비스의 핵심 가치는 순수 벤치마크 점수가 아니라 한국어 언어유희·유머 감각인데, 가격 대비 품질 평가가 최저가 오픈소스 모델군보다 좋다고 판단

**검토했던 대안**:
- **Groq(Llama 계열)**: 속도·가격은 최상급이나 오픈소스 모델이라 한국어 뉘앙스 표현력이 상대적으로 약할 것으로 판단해 1순위에서 제외
- **Jev(TypeSafe AI)**: 분류·판단 전용 "System 1" 모델로 자유 텍스트를 생성하지 않음. 판결문의 핵심인 "이유"/"주문" 서술 자체가 이 모델의 설계 목적과 정반대라 기각. 추후 T/F 판정+확신도만 분리해 Jev로 초저가 처리하고, 서술은 생성형 모델에 맡기는 하이브리드 구조는 2차 최적화 과제로 남김
- **DeepSeek V3.2**: 가성비는 우수하나 데이터 처리·약관 안정성에 대한 우려로 1순위에서 제외
- **Anthropic Claude API / OpenAI API**: Claude API는 실질적 무료 티어가 없고, OpenAI API는 신규 가입 소액 크레딧뿐이라 "카드 등록 없이 바로 시작" 요구사항에 맞지 않음

---

## 레이트리밋 저장소: Cloudflare KV

**결정**: 하루 호출 횟수 카운터를 Cloudflare KV에 저장. 키는 `CF-Connecting-IP` 헤더 기준 IP.

**검토했던 대안 3가지**:

1. **헤더/쿠키 기반, 서버 측 저장 없음**
   - 서버가 카운트를 어디에도 기록하지 않고 클라이언트가 보낸 값(쿠키 등)만 신뢰
   - 기각 사유: 쿠키는 사용자가 개발자도구로 직접 지우거나 조작 가능하고, IP만 보되 서버가 과거 요청 이력을 기억 못 하면 매 요청이 "첫 요청"으로 취급됨. 사실상 레이트리밋이 성립하지 않음

2. **Cloudflare KV** ← 채택
   - 전역 key-value 저장소. `env.RATE_LIMIT_KV.get(ip)` / `.put(ip, count)` 형태로 IP당 오늘 호출 횟수를 저장
   - 무료 티어: 하루 쓰기 1,000회, 읽기 100,000회, 저장공간 1GB — 이 프로젝트 규모에 충분
   - 특성: "최종 일관성(eventually consistent)" — 여러 엣지 리전에서 동시에 같은 키를 쓰면 최대 약 60초 정도 반영이 늦을 수 있음
   - Cloudflare Pages Functions와 네이티브 바인딩 (`wrangler.toml` 설정 몇 줄로 연결, 별도 서버/DB 프로비저닝 불필요)

3. **Durable Object**
   - 강한 일관성(strong consistency) 보장, 카운트 오차 없음
   - 기각 사유: 오버스펙. 실시간 협업/게임 룸처럼 "동시성 충돌이 절대 나면 안 되는" 경우에 적합한 도구이며, 레이트리밋 카운터엔 초 단위 정확도가 불필요. 무료 티어 한도도 KV보다 박함

**판단 근거**: 레이트리밋의 목적은 "정확히 N회"가 아니라 "무료 티어 RPD(하루 1,000회) 총량을 보호"하는 것(기획서 5절 "관리 전략" 참고). KV의 최종 일관성 오차는 이 목적에 전혀 지장이 없고, 설정 난이도와 무료 한도 면에서 가장 합리적.

**남은 세부 결정 (구현 시 확정)**: 하루 허용 횟수(예: 5~10회), KV 키의 TTL(자정 기준 리셋 vs 24시간 롤링).

---

## 민감정보 마스킹 로직

**결정**: Gemini 호출 전, 서버리스 함수 안에서 규칙 기반(공인 블록리스트 + 성씨/조사 휴리스틱) 1차 마스킹 수행. (기획서 4-1절)

**이유**: 사용자 주변 인물의 개인정보가 제3자 API로 그대로 전달되는 것을 줄이고, 실존 공인 이름이 그대로 들어가 콘텐츠가 특정 인물을 저격하는 것처럼 보이는 상황을 방지.

**검토했던 대안**:
- **한국어 개체명 인식(NER) 모델 도입**: 탐지 정확도는 높으나 이 프로젝트 규모에 오버스펙이며 별도 모델 서빙·비용 부담이 생김 → 2차 범위로 보류
- **프롬프트 지시문만으로 방어**("실명을 언급하지 마세요" 등): 모델의 실수나 프롬프트 해석 편차에 단독으로 의존하기엔 불안정 판단, 규칙 기반 마스킹에 더해 이중 방어 수단으로만 채택

**한계 (의도적으로 감수)**: 한글은 대문자 같은 표기 신호가 없어 규칙 기반만으로는 완전한 탐지가 불가능함을 인지하고 진행. 목표는 "완벽 차단"이 아니라 "노출 위험의 실질적 감소".

---

## 개인정보 국외 이전 고지

**결정**: 위 마스킹 로직 + 입력창 하단 고지 문구·동의 체크박스 조합으로 진행.
문구 예시: *"입력하신 내용은 AI 응답 생성을 위해 Google(미국)로 전송되며, 무료 서비스 특성상 서비스 개선에 활용될 수 있습니다."*

**이유**: 개인정보보호법 제28조의8(국외 이전)은 일반 이용약관 동의와 별개로 이전 항목·수신자·국가·목적·보유기간·거부방법을 고지하고 별도 동의를 받을 것을 요구함. 마스킹만으로는 식별 가능성을 완전히 제거하지 못하므로, 고지·동의 절차를 병행하기로 결정.

**검토했던 대안**:
- **고지 없이 무료 티어 그대로 운영**: 구현 비용(체크박스 하나) 대비 규제 리스크가 불균형하게 커서 기각
- **오픈 전 유료 티어로 즉시 전환**(학습 미사용 정책 적용): 비용 부담이 발생하고 프로토타입 검증도 끝나지 않은 단계에서는 과한 조치 판단, 1차 범위에서는 채택하지 않음

**남은 세부 결정**: 정확한 고지 문구는 배포 전 법률 검토 필요 → `backlog.md`

---

## 프론트엔드 디자인 소스

**결정**: [원본 아티팩트](https://claude.ai/artifact/EQzA5QQx8KQEKJLSbQzQH4)를 시각 디자인의 단일 기준(source of truth)으로 확정 (2026-09-24).

**이유**: 기획서에 "신규 UI 추가는 없다"고 명시되어 있어, 별도 디자인 논의 없이 이 아티팩트의 마크업/CSS/애니메이션을 그대로 이식하는 것이 범위 확정에 가장 확실한 기준이 됨.

---

## 프로젝트/서비스 이름

**결정**: 한글 서비스명 "대한팩폭고등법원", 영문/도메인명 "TFcourt"

**이유**: "너 T야?" 밈과 "판결문" 형식을 결합한 컨셉에서, 성씨 언어유희형 조합보다 "일단 리듬감 있게 짓고 의미는 나중에 갖다 붙이는" 방식이 더 재미있다는 판단(초기 네이밍 실험 결과)을 바탕으로, "팩폭"(직설적 유머 코드)을 그대로 살리는 쪽으로 최종 결정. "지방법원" → "고등법원"으로 격상해 더 권위 있는(그래서 더 우스운) 톤으로 조정.

**검토했던 대안**: 성씨 합성어 방식("장안메밀" 류), 밈 문구 직접 인용형("너티야지방법원" 등) — 전부 후보로 검토했으나 임팩트·기억 용이성 면에서 "팩폭"이 최종 채택됨.

---

## 도메인

**결정**: 1차 배포는 Cloudflare Pages 무료 서브도메인(`프로젝트명.pages.dev`)을 그대로 사용. 커스텀 도메인 구매는 트래픽 반응을 지켜본 뒤 추후 결정.

**이유**: 무료로 즉시 시작 가능하고 SSL도 자동 적용되어 검증 단계에 충분함. 반응 확인 전에 도메인부터 구매하는 것은 시기상조로 판단.

**검토했던 대안**:
- **`.com`**: 제약 없고 무난하나, 원하는 이름(예: `TFcourt.com`)이 이미 등록돼 있을 가능성이 높아 실시간 확인이 필요
- **`.io`**: 테크 프로덕트 느낌은 있으나, 영국-모리셔스 간 차고스 제도(Chagos Islands) 주권 이전 이슈로 국가 코드 자체의 장기 존속에 불확실성이 있어 우선순위를 낮춤
- **`.kr`**: 한국 서비스 정체성은 명확히 전달되나, 검토한 세 확장자 중 등록비가 가장 비쌈(연 10만원대)
- **js.org 등 무료 서브도메인 서비스**: JavaScript 생태계와 직접 관련된 프로젝트만 승인하는 정책이라 이 서비스 성격상 부적합 판단, 기각
- **향후 정식 도메인 구매 시 1순위 후보**: Cloudflare Registrar — at-cost 가격(.com 기준 연 $10.44), WHOIS 개인정보보호 기본 포함, Pages와 같은 계정이라 DNS 연결이 자동으로 이루어짐

---

## 개발 언어

**결정**: 서버리스 함수(`/api/judgment`)는 TypeScript로 작성. 프론트엔드는 현재 규모상 바닐라 JavaScript 유지.

**이유 — 서버리스 함수는 TypeScript**:
- Cloudflare Pages Functions가 `.ts` 파일을 별도 빌드 설정(webpack, babel 등) 없이 네이티브로 지원 — 도입 비용이 사실상 0
- Gemini API 요청/응답 형식, 판결 JSON 스키마(`verdict`, `score`, `charge`, `ruling`, `reasoning`, `quote`)처럼 **구조가 고정된 데이터**를 다루는 코드라 타입 정의로 얻는 실수 방지 효과가 큼

**이유 — 프론트엔드는 바닐라 JavaScript (TypeScript 미도입)**:
- 페이지 규모가 단일 HTML 파일 + 상태 값 몇 개(사연 입력, 판결 결과, 로딩 여부) 수준이라, 컴포넌트 트리나 복잡한 상태관리가 필요하지 않음. 이 규모에서 타입 시스템이 막아주는 버그보다, 빌드 도구가 추가하는 복잡도가 더 큼
- 브라우저는 TypeScript를 직접 실행하지 못하므로, 프론트에 TS를 쓰려면 Vite 같은 번들러를 새로 도입해야 함 — Pages Functions처럼 "그냥 되는" 게 아니라 빌드 파이프라인·의존성 관리 비용이 새로 생김
- 이미 검증된 프로토타입(바닐라 JS, [원본 아티팩트](https://claude.ai/artifact/EQzA5QQx8KQEKJLSbQzQH4))이 존재하는 상태 — 굳이 다시 쓰는 재작업 비용을 들이지 않고 그대로 재사용하는 쪽을 선택
- 결과적으로 프론트/백엔드의 TS 도입 비용이 비대칭적(백엔드는 0에 가깝고 프론트는 새 도구가 필요함)이라는 점이 서로 다른 결정을 내린 핵심 근거

**대안이었던 것**: 프론트도 TypeScript + Vite로 통일 — 코드베이스 일관성은 얻지만, 이 프로젝트 규모에서는 실익보다 설정 비용이 커서 기각. 트래픽·기능이 늘어 상태 관리가 복잡해지는 시점에 재검토.

---

## API 엔드포인트 네이밍

**결정**: `/api/judgment`

**이유**: API 경로는 최종 사용자가 아니라 개발자(및 Claude Code)가 보는 코드이므로, verdict/judgment 사이의 뉘앙스 차이(캐주얼함 vs 공식성)보다 명명 선호가 우선한다고 판단. `/api/tfcourt`처럼 서비스명을 경로에 또 넣는 중복은 여전히 피함 — 기능 중심 네이밍(`judgment` = 판결을 반환) 원칙은 유지.

**참고**: 응답 JSON의 필드명도 `judgment: "T"|"F"`로 통일 (엔드포인트 경로와 일관성 유지). 프로토타입([원본 아티팩트](https://claude.ai/artifact/EQzA5QQx8KQEKJLSbQzQH4))에도 동일하게 반영됨 (2026-09-24).

---

## 보류 항목

- 개인정보 국외 이전 고지의 **정확한 법률 문구** (마스킹 + 동의 체크박스라는 메커니즘 자체는 위 "개인정보 국외 이전 고지" 항목에서 이미 결정됨) → `backlog.md`로 분리
- 레이트리밋 하루 허용 횟수, KV 키의 TTL 정책 (위 "레이트리밋 저장소" 항목의 "남은 세부 결정" 참고) → 구현 시 확정 (2026-09-24)
